● SPACE DASH 게임 기능 정리

● 게임 개요 (Terminal ASCII 레이싱/회피)

○ 터미널(콘솔)에서 실행되는 ASCII 기반 레이싱/생존 게임
○ 트랙이 위로 스크롤되고, 플레이어는 좌우 이동으로 장애물을 피하면서 진행
○ 목표(GOAL) 라인 도달 시 클리어 / 충돌 시 게임 오버



● 모드 구성 (NORMAL / HARD)

○ NORMAL 모드
기본 난이도

장애물(바위/콘 등) 충돌 시 GAME OVER
아이템 점수 시스템 적용(★/●/▲)
최고점수 파일 저장(Highscore)

○ HARD 모드

NORMAL 구조 기반 + 난이도 강화
미사일 시스템 추가(핵심 차별점)
미사일 피격 시 즉시 GAME OVER
최고점수 파일 저장(Highscore)


● 조작/입력 시스템

○ 키보드 입력 기반 실시간 조작

이동: A/D 또는 ←/→

종료: ESC

○ 입력 감지 방식

로비(메뉴)에서 keyboard.is_pressed() 기반 입력 감지

게임 루프에서도 키 상태를 읽는 함수로 입력 처리(예: get_key_state())





● 시작 UI / 모드 선택 UI (로비)

○ 시작 화면에서 모드 선택 UI 제공

선택 인덱스 이동 → 선택 확정 방식

화면 중앙 정렬 출력 지원

○ 관련 함수(메인 UI 출력)

print_centered(), print_centered_end(), print_block_centered()

글자 폭 보정: disp_width(), truncate_to_width(), pad_right_to_width()

메뉴 화면: screen_one() (메인에서 모드 선택)







● 게임 시작 연출 (3, 2, 1, START!)

○ 게임 시작 전 카운트다운 연출로 몰입감 강화

“3 → 2 → 1 → START!” 출력 후 시작

단순 텍스트가 아니라, 큰 글자(아스키 아트 느낌) 출력 방식

○ 관련 함수/구현 요소

메인 쪽: get_big_start_text() (큰 START 텍스트 구성)

모드 내부 연출: countdown_on_map() 같은 방식으로 “맵 위에 얹어서” 카운트다운


● 트랙(맵) 시스템 (ASCII 트랙 파일 기반)

○ 트랙은 파일로 분리되어 유지보수 가능

NORMAL: track.txt
HARD: track_hard.txt (하드 전용 트랙 구조)


○ 트랙 스크롤 구조

scroll_i 같은 인덱스가 증가하면서 “현재 화면에 보이는 트랙 구간”을 잘라서 렌더링

○ 트랙 내 벽 판별/도로 영역 계산

벽 문자(예: │, |)를 기준으로 도로 영역(road bounds)을 찾아 안전하게 배치

관련 함수:

_find_walls_in_row()
get_road_bounds_safe()



● 미니맵 시스템 (인게임 진행도 표시)

○ 우측 사이드바에 미니맵 표시

전체 트랙 대비 현재 위치를 압축해서 보여줌

진행도/위치 파악이 빨라져 플레이 감이 좋아짐(UX 개선)

○ 미니맵 생성/압축 로직 (핵심)

트랙을 작은 폭으로 압축하는 방식(“대표 문자 뽑기 + 구간 압축”)

관련 함수:

build_track_ascii_minimap()

_mini_pick_char()

_compress_segment()

○ 관련 변수(미니맵 품질/크기 튜닝 포인트)

MINI_INNER_W, MINI_X_SHRINK, MINI_Y_SHRINK

(중요 문자 세트) _MINI_IMPORTANT, _MINI_SPACEY




● 점수 시스템 (시간 + 아이템)

○ 점수는 “시간 진행” + “아이템 획득”이 합쳐져서 증가

일정 시간마다 점수 누적(생존 점수)

아이템 획득 시 추가 점수/감점

○ 아이템 종류/점수

★ : +5

● : +3

▲ : -2

○ 관련 구현 요소

아이템 스폰:

choose_item_spawn() : 벽 안쪽 안전한 도로 영역에 스폰 위치 선정

아이템 충돌/반영:

플레이어 셀 집합과 아이템 좌표가 겹치면 점수 처리 후 아이템 제거



● 장애물 시스템 (Normal 중심)

○ 장애물 종류

바위/콘 스프라이트(ASCII 아트)

○ 트랙에 장애물을 “박아넣는(스탬프)” 방식

트랙의 도로 영역을 찾아서 장애물 아트를 라인에 합성

관련 함수(장애물 생성/합성)

get_rock(), get_cone()

_stamp()

add_obstacles_to_track()

○ 충돌 판정

플레이어가 장애물과 겹치면 GAME OVER





● HARD 전용: 미사일 시스템 (너의 핵심 기여)

○ 미사일 형태

MISSILE_SHAPE = "<====" 처럼 문자열 스프라이트 사용

○ 미사일 생성/이동/삭제 흐름

일정 조건에서 미사일 생성 → 리스트에 저장

프레임마다 좌측으로 이동시키며 화면에 그림

화면 밖으로 나가면 제거(메모리/리스트 관리)

○ 미사일 충돌 판정(진짜 중요한 포인트)

플레이어 스프라이트를 “좌표 셀 집합(set)”으로 만들고

미사일도 동일하게 셀 집합을 만들어서
집합 교집합(car_cells & missile_cells) 으로 충돌 판정



● 하이스코어(최고점) 저장

○ 모드별로 하이스코어 파일 분리 저장

NORMAL: highscore_normal.txt

HARD: highscore_points.txt

○ 관련 함수

load_highscore()

save_highscore()






● 화면 처리 / 깜빡임 최소화 / ANSI 제어

○ 화면 갱신은 콘솔 특성상 깜빡임이 생기기 쉬워서 제어 로직 포함

화면 클리어: clear_screen()

윈도우 ANSI 활성화: enable_ansi_on_windows()

커서 숨김/표시: hide_cursor(), show_cursor()

커서를 홈으로 이동: move_cursor_home()

○ 사이드바 포함 렌더링

render_with_sidebar() 로 “왼쪽 트랙 + 오른쪽 UI/미니맵” 구성







내가 맡은 역할 (개인 기여 정리)

○ UI 기여 (약 30%)

시작 화면 / 텍스트 중앙정렬 / 출력 구조 일부 참여

출력 폭 보정 함수 사용(한글/특수문자 폭 고려)

○ 트랙맵 제작

track.txt 트랙 데이터 직접 제작

트랙 구조(벽/도로) 설계를 고려해서 플레이 가능한 레이아웃 구성

○ 하드모드 제작(핵심)

HARD 모드 전체 게임 흐름에서 미사일 시스템 설계/구현

미사일 생성/이동/삭제/충돌 판정(셀 집합 교집합 방식) 구현

○ 게임 시작 연출 제작

3,2,1 START! 카운트다운 연출 제작/적용

○ 인게임 미니맵 아이디어 제공

미니맵을 사이드바에 넣는 UX 아이디어 제안

트랙 압축 방식의 미니맵 구현 구조에 기여

○ 점수 시스템 추가제 제작

시간 기반 점수 누적 구조 + 아이템 점수 가감(★/●/▲) 구조 설계/반영

Normal/Hard 모두 점수 개념 확장에 참여

○ 그 외(나머지 기술 요소)

장애물 상세 로직, 일부 렌더링 확장, 기타 기능은 팀원들이 담당







★내가 구현한 기능 및 Python 기술 정리★

● 게임 시작 연출 (3, 2, 1, START!)

○ 게임 시작 전 카운트다운 연출 구현
○ ASCII 아트 기반 큰 글자 출력
○ 카운트다운을 트랙 화면 위에 합성하여 출력
○ time.sleep()을 이용해 단계별 출력 시간 제어
○ 문자열 → 2차원 grid 변환 후 중앙 좌표에 덮어쓰기 방식 적용

사용 기술

리스트 / 딕셔너리 기반 ASCII 아트 관리

2차원 리스트(grid) 조작

좌표 계산(중앙 정렬)

프레임 단위 화면 합성

● HARD 모드 미사일 시스템 (핵심 구현)

○ HARD 모드 전용 공격 요소(미사일) 설계 및 구현
○ 문자열 스프라이트(<====) 기반 미사일 표현
○ 미사일 생성 → 이동 → 삭제 전체 흐름 구현
○ 미사일 좌표를 절대좌표(abs_y)로 관리하여 스크롤과 자연스럽게 연동
○ 화면 밖 미사일 자동 제거 처리

충돌 판정 방식

플레이어와 미사일을 각각 문자 좌표 집합(set) 으로 변환

두 집합의 교집합 여부로 충돌 판정

충돌 시 즉시 GAME OVER 처리

사용 기술

리스트 + 딕셔너리 자료구조

set 자료형 기반 충돌 판정

실수(float) 좌표 이동 처리

안전한 리스트 갱신 방식

● 트랙맵 제작 (ASCII 맵 데이터)

○ Normal / Hard 모드용 트랙 맵 직접 제작
○ 벽(│)과 도로 영역을 고려한 구조 설계
○ 장애물, 아이템, 미사일 배치가 가능한 맵 형태로 구성
○ 트랙 파일 분리 관리(track.txt, track_hard.txt)

사용 기술

파일 기반 맵 데이터 관리

ASCII 문자 기반 레이아웃 설계

도로/벽 구분 구조 설계

● 인게임 미니맵 아이디어 및 설계 반영

○ 전체 트랙 대비 현재 위치를 보여주는 미니맵 아이디어 제안
○ 우측 사이드바에 압축된 트랙 정보 표시
○ 진행률(%) 계산 및 표시
○ 화면 크기에 따라 미니맵 높이 자동 조절

구현 구조 특징

트랙을 가로/세로로 압축하여 표현

현재 차량 위치를 미니맵에 오버레이

상수 값으로 미니맵 크기 및 해상도 조절 가능

사용 기술

문자열 슬라이싱 및 압축 처리

좌표 비율 계산

UI 정보 오버레이 방식

● 점수 시스템 확장 (Normal / Hard)

○ 시간 기반 점수 누적 구조 설계
○ 초 단위로 점수 증가하여 프레임 영향 최소화
○ 아이템 획득 시 점수 가감 구조 추가
○ 점수 음수 방지 처리

사용 기술

시간 계산(time.time())

조건 기반 점수 갱신 로직

점수 안정성 처리

● 콘솔 UI 출력 기여 (약 30%)

○ 터미널 중앙 정렬 출력 구조 일부 구현
○ 한글/특수문자 출력 폭을 고려한 정렬 처리
○ 정보 박스 및 텍스트 정렬 가독성 개선

사용 기술

shutil.get_terminal_size() 기반 화면 크기 계산

unicodedata.east_asian_width()를 이용한 문자 폭 계산

문자열 패딩 및 잘림 처리

● 개인 기여 요약

○ HARD 모드 핵심 기능(미사일 시스템) 구현
○ 게임 시작 카운트다운 연출 제작
○ ASCII 트랙맵 데이터 설계 및 제작
○ 인게임 미니맵 UI 아이디어 제안 및 구조 반영
○ 시간 + 아이템 기반 점수 시스템 확장
○ 콘솔 UI 출력 구조 일부 구현 (약 30%)








